# Журнал разработки (Progress & Dev Diary)

## [29.06.2026] Фикс подключения WhatsApp MCP к n8n (Ошибка: Cannot POST /sse)

**Проблема (Issue):**
При попытке подключить Evolution MCP сервер к n8n через SSE (Server-Sent Events) клиент n8n выдавал ошибку `Streamable HTTP error: Error POSTing to endpoint: Error Cannot POST /sse`. 
Это происходило потому, что клиент n8n почему-то отправлял POST-запросы с JSON-RPC payload прямо в корень подключения `/sse`, игнорируя правильный эндпоинт `/message`, который выдает стандартный `SSEServerTransport` из `@modelcontextprotocol/sdk`.

**Решение (Win):**
Был произведен прямой вход на VPS по SSH.
В файле `/opt/mcp-evolution-whatsapp-api/src/sse.ts` маршрутизатор Express был изменен, чтобы принимать POST-запросы как на `/message`, так и на `/sse`.
```typescript
// Старый код:
app.post("/message", async (req, res) => { ... })

// Новый код:
app.post(["/message", "/sse"], async (req, res) => { ... })
```

**Критический нюанс (Важный опыт):**
Изначально изменение файла и выполнение команды `docker restart mcp-evolution-whatsapp` не дало результата. 
**Причина:** Docker кэширует копирование файлов (`COPY . .`) на этапе сборки (`docker build`). Простой рестарт контейнера запускает старый кэшированный код.
**Правильный фикс:** Потребовалось полностью пересобрать образ (`docker build -t mcp-evolution-whatsapp -f Dockerfile.sse .`) и пересоздать контейнер (`docker run ...`). 

После полной пересборки интеграция успешно заработала. Баг n8n обойден на стороне сервера.

## [30.06.2026] ИИ-Юрист: Выпуск ЭЦП и обход NCALayer

**Достижения (Wins):**
- Успешно проведен квест по удаленному выпуску ЭЦП через did.gov.kz для бота.
- Получены файлы `GOST512...p12`.
- Файлы безопасно перемещены в папку `keys/` и добавлены в `.gitignore`, чтобы избежать утечки в репозиторий.
- Пароль от ключей надежно сохранен в `.env` (переменная `ECP_PASSWORD`).
- Написан скрипт `send_keys_to_tg.py` для пересылки ключей пользователю в Telegram-бот.

## [30.06.2026] Устойчивость ежедневной лидогенерации и оптимизация launchd

**Достижения (Wins):**
- Устранены дубликаты plist-файлов в `~/Library/LaunchAgents/` (удалены `com.higherpower.daily.leadgen.plist` и `com.higherpower.leadgen.plist`), предотвратив двойной запуск пайплайна и лишний расход API-токенов.
- Внедрен импорт `signal` и перехват `SIGHUP` во всех скриптах пайплайна (`run_pipeline.py`, `playwright_leadgen.py`, `daily_leadgen.py`).
- Добавлен перехват `BrokenPipeError` и `OSError` при операциях записи в `sys.stdout` внутри `run_pipeline.py`. Это делает процессы неубиваемыми при закрытии окна Терминала пользователем.

**Решенные проблемы (Wins):**
- **Полное устранение TCC PermissionError в macOS:** Устранена критическая ошибка `Operation not permitted` при фоновом запуске launchd. Решено путем перевода `ProgramArguments` в plist на вызов `osascript`, который запускает wrapper-скрипт через `Terminal.app` в контексте Aqua-сессии пользователя. Это дает Python полный доступ к Рабочему столу (`Desktop`) и виртуальному окружению `.venv`.
- **Автоматизация во wrapper-скрипте:** Обновлен скрипт `06_Scripts_and_Tools/setup_scheduler.py`. Теперь он автоматически генерирует исполняемый `run_daily_pipeline_wrapper.sh` в домашней папке пользователя, динамически определяет пути к Python и регистрирует правильный LaunchAgent `com.higherpower.daily_leadgen.plist`. Скрипт проверен и успешно запускает ежедневный сбор в 09:00, сохраняя логи выполнения в `~/Library/Logs/`.

## Следующие шаги:
- Проанализировать результаты сегодняшнего сбора лидов за 30 июня.
- Начать разработку/интеграцию Playwright скрипта для авторизации в Судебном кабинете с помощью `pykalkan` (без окон NCALayer).

## [04.07.2026] Верификация ежедневной лидогенерации и стабильная работа Vertex AI

**Достижения (Wins):**
- **Успешный автоматический и ручной прогон пайплайна:** Полностью верифицирована работа пайплайна. Сегодняшний запуск обработал **67 лидов** (hh.ru — 25, adata.kz — 32, hh.kz — 8, threads.net — 2).
- **Надежный ИИ-фолбек:** Подтверждена стабильная работа ИИ-обогащения через Vertex AI (Gemini 2.5 Flash по HTTP с использованием локального OAuth2 токена авторизации). Все лиды обогащены без ошибок, лимиты API OpenAI не превышены.
- **Генерация предложений:** Для каждого лида в директории `03_Marketing_and_Sales/daily_leads/2026-07-04/details/` создана индивидуальная карточка с болями и драфтом сообщения на русском языке (стиль Ника Сараева).
- **Telegram Уведомления:** Сводный отчет и топ-5 самых горячих лидов с прямыми ссылками и контактами успешно отправлены в Telegram-канал пользователя.

## [30.06.2026] Аудит и верификация пайплайна ежедневной лидогенерации

**Достижения (Wins):**
- **Полная проверка функционала сбора лидов:** Подтверждена стабильная работа сбора лидов со всех 4 источников (hh.ru, hh.kz, adata.kz, threads.net) по всем 6 ключевым запросам пользователя ("ии", "разработка", "боты", "маркетинг", "контекстная реклама", "ии контент").
- **Анализ результатов запуска от 30.06.2026:**
  - Всего успешно собрано и обработано **75 лидов**.
  - Из них: `hh.kz` — 7, `hh.ru` — 30, `adata.kz` — 31, `threads.net` — 7.
  - Успешно сформирован CSV-свод и детальные Markdown-карточки для каждого лида в директории `03_Marketing_and_Sales/daily_leads/2026-06-30/details/`.
  - Каждый лид снабжен ИИ-оценкой релевантности (AI Score), анализом болей, списком предлагаемых услуг и готовым персонализированным шаблоном первого сообщения.
- **Успешный тестовый запуск:** Проведен контрольный запуск скрипта `scripts/test_single_run.py` во внутреннем виртуальном окружении `.venv`. Сбор прошел штатно, селекторы HH и Adata актуальны, Playwright успешно обошел базовые анти-бот защиты.
- **Интеграция CRM и уведомлений:** Лиды с контактами успешно записаны в Supabase, а ТОП-5 самых горячих лидов с готовыми драфтами отправлены в рабочий Telegram-чат.

## Следующие шаги:
- Начать интеграцию скрипта авторизации в Судебном кабинете с использованием `pykalkan`.

## 2026-07-01: Интеграция с Судебным кабинетом
- [x] Настроен скрытый проход авторизации через NCALayer (auth_stealth.py).
- [x] Реализован обход скрытых элементов Selectize.js для выбора года и категории (Трудовые споры).
- [x] Успешно получен список дел (1136 результатов за 2024 год).
- [ ] Экспорт данных в CSV/JSON.

## [01.07.2026] ИИ-Юрист: Стабилизация KalkanCrypt и обход NCALayer

**Проблема (Issue):**
При попытке инициализировать C-библиотеку KalkanCrypt (`libkalkancryptwr-64.so.2.0.2`) на сервере для фонового обхода NCALayer через скрипт `pykalkan` возникала ошибка `Exit 139 (SIGSEGV)` - Segmentation Fault.
Причина крылась в конфликте встроенной `libkalkancrypto.so` с системными библиотеками OpenSSL (двойная инициализация OpenSSL) и нехватке зависимостей (`libiconv`, `libpcsclite`).

**Решение (Win):**
- Удалили предзагрузку изолированной `libkalkancrypto.so`.
- В `Dockerfile` для `AlmaLinux 8` добавили сборку `libiconv` из исходников (GNU) и установку `pcsc-lite`.
- Переработали скрипт `auth_kalkan.py`: убрали неверный вызов неэкспортируемой функции `KC_Init`, так как инициализация KalkanCrypt происходит неявно при вызове `KC_LoadKeyStore`.
- Все библиотеки загружаются с флагом `ctypes.RTLD_GLOBAL` для корректной линковки символов.

**Следующие шаги:**
- Дождаться успешного завершения сборки Docker-образа и тестового прогона.
- Интегрировать логику работы с ЭЦП напрямую в `parser_tk.py` для API авторизации в Судебном кабинете.

## [01.07.2026] ИИ-Юрист: Авторизация в Судебном кабинете (AJAX + KalkanCrypt)

**Достижения (Wins):**
- Полностью решена проблема авторизации в office.sud.kz через ЭЦП без использования браузера и NCALayer.
- Реализован C-wrapper (`kalkan_sign.c`) для вызова нативного KalkanCrypt SDK (AlmaLinux 8 Docker).
- Успешно пройдена сложная JSF/RichFaces AJAX аутентификация.
- Решены проблемы с 0x8f0003b (настроен `update-ca-trust` с корневыми сертификатами НУЦ).
- Создан подробный гайд `docs/sud_kz_auth_guide.md` с описанием всех подводных камней и решений.
- Бот успешно скачивает список дел из закрытой части кабинета.

**Следующие шаги:**
- Детальный парсинг ссылок на документы (через BeautifulSoup).
- Настройка RAG (векторизация скачанных судебных актов).


## [02.07.2026] Успешная лидогенерация и обнаружение инфраструктурных блокировок

**Достижения (Wins):**
- **Автоматический сбор лидов работает стабильно:** Проверен сегодняшний запуск от 2026-07-02. Шедулер (launchd) успешно отработал в 09:00 через обертку `Terminal.app` в Aqua-сессии.
- **Сбор и обработка 76 лидов за день:** Собраны данные с `hh.ru` (30), `hh.kz` (7), `adata.kz` (32) и `threads.net` (7) по всем 6 запросам пользователя ("ии", "разработка", "боты", "маркетинг", "контекстная реклама", "ии контент").
- **Результаты полностью структурированы:** Сформирован отчет `leads_report.md` в папке `03_Marketing_and_Sales/daily_leads/2026-07-02/`, создана таблица с ИИ-оценками и индивидуальные Markdown-карточки лидов с офферами, болями и готовыми разговорными драфтами в `details/`.

**Проблемы и риски (Issues & Blockers):**
- **Переполнение диска (no space left on device):** При попытке запуска интерактивных терминальных команд через `run_command` система выдает ошибку записи временного файла, что указывает на переполнение диска на локальном Mac пользователя. Необходимо провести очистку (удалить старые медиа/логи).
- **Отсутствие контактных телефонов (скрытые контакты на Adata.kz):** На сайте pk.adata.kz контакты (телефоны/email) большинства компаний скрыты за плашкой авторизации ("Доступно после входа"). Поскольку Playwright-парсер заходит без авторизации, он собирает лиды со значком 🚫. Требуется настроить авторизацию в парсере или использовать `pk.uchet.kz` с обходом капчи, как описано в навыке `b2b-lead-enricher`.


