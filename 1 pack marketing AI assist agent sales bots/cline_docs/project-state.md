Проект: 1 pack marketing AI assist agent sales bots
Завершённость: [██████████░░░░░░░░░░] 50% — backend parsing & validation

## Текущая задача
Исправление таймаута судебного парсера и оптимизация ежедневного пайплайна сбора лидов

## Что уже сделано
1. Диагностирована ошибка таймаута (`Page.wait_for_selector: Timeout 15000ms exceeded` для `#filter-button`) в парсере `scripts/sud_parser/parser_tk.py`.
2. Найдена причина: форма логина обновила свой ID с `j_idt70:auth` на `j_idt74:auth`, из-за чего проверка авторизации ложно возвращала `True` и пыталась искать фильтр на странице входа.
3. Исправлена логика проверки авторизации: теперь статус сессии проверяется по поиску кнопок "Выход"/"Шығу", как в stealth-скрипте.
4. Добавлен корректный выход из Python-процесса с кодом `1` при невалидной сессии, чтобы оркестратор `run_pipeline.py` не продолжал работу при ошибке.
5. Изменения протестированы локально и закоммичены в git.
6. Обновлен лог изменений `log.md` с описанием побед и ошибок на двух языках (русском и английском).

## Ключевые находки
- Проверка авторизации на динамических JSF-страницах `sud.kz` стабильнее всего работает через поиск текстовых индикаторов сессии ("Выход" / "Шығу"), так как ID элементов форм авторизации меняются динамически.
- Логика сбора лидов с hh.kz/hh.ru при обогащении контактов через adata.kz автоматически перенаправляет лиды в `external_leads.json`, так как их источник меняется на `adata.kz` для последующей фильтрации. Это корректно и не является багом.

## Следующий шаг
1. Провести мониторинг ежедневного запуска пайплайна (убедиться, что он вовремя прерывается или завершается без зависаний).
2. Обновить токен авторизации Судебного кабинета (пользователю нужно запустить `python scripts/sud_parser/auth_stealth.py` для пересохранения кук в `sud_state.json`, если сессия устарела).
3. Перейти к следующей приоритетной задаче из KANBAN.md или Backlog.